class Template {
    constructor(id, title, description, isPublic) {
        this.id = id;
        this.title = title;
        this.description = description;
        this.isPublic = isPublic;
    }
}

export default Template;

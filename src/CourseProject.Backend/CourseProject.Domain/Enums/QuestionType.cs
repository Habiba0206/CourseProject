namespace CourseProject.Domain.Enums;

public enum QuestionType
{
    SingleLineString, // For single-line string questions
    MultiLineText, // For multi-line text questions
    PositiveInteger, // For questions expecting positive integers
    Checkboxes // For questions with multiple choices
}
